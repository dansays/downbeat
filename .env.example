# Discogs — used to seed your taste rubric from your record collection.
# Username is your Discogs handle. Token is a personal access token from:
#   https://www.discogs.com/settings/developers
DISCOGS_USERNAME=
DISCOGS_TOKEN=

# Todoist — used to create "review" tasks for matching shows.
# Token from: Todoist → Settings → Integrations → Developer → API token
TODOIST_TOKEN=

# Name of the Todoist project that review tasks land in (created if missing).
TODOIST_PROJECT=Events & Entertainment

# Last.fm — free API key, powers the "listen ahead" Apple Music song list (/sync-playlist).
# Get one at https://www.last.fm/api/account/create
LASTFM_API_KEY=

# --- /dj-show (Roon) -------------------------------------------------------
# Builds a curated Roon playlist of upcoming matched artists. Roon must be on the same network and
# the "Downbeat DJ" extension enabled once under Roon → Settings → Extensions.

# Name of the Roon playlist /dj-show creates (default: "Downbeat — Late Night").
ROON_PLAYLIST_NAME=

# Optional: where the paired-core token is cached (default: data/.roon-state).
# ROON_STATE_DIR=

# --- Phase 2 (DJ commentary; not used by the music-only pipeline yet) ------
# ELEVENLABS_API_KEY=
# ELEVENLABS_VOICE_ID=XFQFwy8OEb9lvFQIMZ5a   # "Boe Deepman"
# ELEVENLABS_MODEL=                          # default mp3_44100_128
# ROON_DJ_CLIPS_DIR=                         # must be a Roon-watched folder
