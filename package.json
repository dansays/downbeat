{
  "name": "downbeat",
  "version": "0.1.0",
  "private": true,
  "description": "Find live jazz shows you'll actually like and file them as Todoist review tasks.",
  "type": "module",
  "bin": {
    "downbeat": "src/cli.ts"
  },
  "scripts": {
    "downbeat": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
